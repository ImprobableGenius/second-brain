# Wednesday Plan - 2026-04-01

## First Block
- Move directly into `BANF-07` and `BANF-08`.
- Goal: reduce Banff release risk first because the release lane is now the nearest hard-pressure work on the board.

## Second Block
- Use the MASC block on `MASC-16` first.
- If the newer dataset files are sourced cleanly, move into the next unblocked MASC remediation item:
  - `MASC-13`
  - then `MASC-12`
- Do not reopen `MASC-08` unless the design and client inputs actually land.

## Third Block
- Move into `IPAM-01`.
- Goal: make bounded progress on member object modeling instead of reopening the whole IPAM lane at once.

## Float Block
- If Banff release work closes and the MASC unblock stays contained, use remaining time on:
  - `IPAM-02`
  - `IFWH-02`
  - `NWAC-01` only as a contained documentation cleanup block

## Guardrails
- Keep Banff first because release and deployment are already carrying deadline pressure.
- Keep the MASC block on unblocked work only; `MASC-08` remains blocked until design and client input arrive.
- Keep IPAM to a focused modeling pass, not broad frontend churn.
