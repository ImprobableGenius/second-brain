---
title: "Tuesday Plan - 2026-04-07"
---

## First Block
- Move directly into:
  - `IPAM-03`
- Goal: keep the active `feat/file-upload` branch moving and turn the current upload implementation into a cleaner milestone instead of leaving it as pure progress.

## Second Block
- Stay in the IPAM lane on:
  - `IPAM-03`
  - then `IPAM-04` if the upload work stabilizes cleanly
- Goal: use today to reduce the April 15 risk where the code is already hot, rather than context-switching too early.

## Third Block
- Use one contained MASC block on:
  - `MASC-21`
- Goal: clear the new `programDetails.xml` email follow-up without reopening the broader MASC backlog.

## Fourth Block
- Move into one contained Banff block:
  - `BANF-05`
- Goal: keep Banff moving early against the April 20 date without letting it displace the tighter IPAM lane.

## Float Block
- If the main blocks land cleanly, use remaining time on:
  - `BANF-06`
  - `PERM-01` only if the colleague-support lane needs the remaining widget/PR cleanup
  - `MASC-13` only if there is still uninterrupted build time after the above

## Guardrails
- IPAM is the main deadline-risk lane now; protect it first.
- Keep Banff moving, but as a contained second lane rather than the primary lane today.
- Keep MASC limited to `MASC-21` and avoid reopening `MASC-17` through `MASC-20` unless an explicit decision is made.
- Do not let Perimeter expand beyond the current `PERM-01` widget/PR lane.
- If `IPAM-03` reveals significant storage or admin-integration gaps, capture them explicitly instead of silently absorbing them into the day.
