---
title: "Today Plan - 2026-04-13"
---

## Focus
- Primary lane: finish the remaining launch-blocking MASC audit work and convert it into a clean implementation handoff.
- Secondary lane: protect real IPAM execution time because the staging target is still `2026-04-15`.
- Deferred lane: Banff stays parked unless MASC and IPAM both move cleanly.

## Order

### First Block
- `MASC-23`
  - close the remaining scripting audit tail on rows `76` and `79`
  - capture the implementation outcome for the hard-coded and missing-path findings
  - do not widen this into full remediation work yet; finish the audit and handoff first

### Second Block
- `IPAM-04`
  - protect one uninterrupted admin-review-tools block
  - prioritize the smallest meaningful admin slice that moves the review workflow forward

### Third Block
- `IPAM-05`
  - use the next protected block to clarify or implement the export and payment-marker lane
  - if `IPAM-04` exposes a blocker, use this block to resolve the tightest dependency rather than switching projects

### Fourth Block
- `MASC-13`
  - only after `MASC-23` and the protected IPAM block land
  - keep scope to the shared overview-nav gradient treatment only

### Float
- `MASC-12` only if the gradient fix is stable and the image-size change stays contained
- `MASC-04` only if the scope is clarified without reopening a design or content rabbit hole
- Banff only if both MASC and IPAM land early and there is real overflow capacity

## Guardrails
- Do not open `MASC-07` today.
- Do not let `MASC-23` silently turn into a full implementation bundle.
- Do not spend primary time on Banff while both MASC and IPAM are still carrying April delivery pressure.
- Do not widen `MASC-13` or `MASC-12` into a broader hero redesign.

## Success Condition
- Best case:
  - close `MASC-23`
  - materially advance `IPAM-04`
  - either clarify or start `IPAM-05`
  - optionally close `MASC-13` if it proves to be a clean shared fix
- Acceptable day:
  - fully close `MASC-23`
  - plus one real protected IPAM execution block
