# Tomorrow Plan - 2026-03-25

## First Block
- Clear the two `MASC-D02` discussion blockers, then close the overview-page bundle:
  - confirm better wording for `View Land Parcel Info`
  - review hero-image alignment with Kali before making the final call

## Second Block
- Move directly into `MASC-D03` once `MASC-D02` closes.

## Response Block
- `BANF-10`: finish the pending Banff WPML URL / slug follow-up from today.
- `MASC-10`: send the short Tristin response on the missing dataset-path options:
  - `allCrops.xml / Basic Hay / varieties / Low Dollar Value`
  - `allCrops / crop Wheat / variety Winter Wheat / per_bushel`

## Float Block
- If capacity opens after the main MASC and Banff work, use the remaining time on:
  - `IDE-01`: review the `Initiative Hero` circular image source and the `Download Brochure (PDF)` button workflow, then reply to the client with the exact update path
  - `MASC-08`: restore the circular bin volume calculator widget on the `Insurance with MASC` page
  - `MASC-11`: follow up on the missing calculator block on the `Borrowing with MASC` page

## Guardrails
- Keep `BANF-10` and `MASC-10` bounded to response work, not implementation.
- Keep `IDE-01` bounded to a support reply and update-path investigation. Do not let it turn into implementation unless the ask changes.
- Keep `MASC-08` and `MASC-11` scoped to the missing calculator-block issues only. Do not let them expand into a broader calculator-pages redesign.
- Keep `IFWH-01` and `NWAC-01` visible, but do not let them displace the active MASC lane unless priorities change.
